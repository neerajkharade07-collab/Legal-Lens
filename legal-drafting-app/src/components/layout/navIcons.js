import {
  PenLine,
  FolderOpen,
  LayoutTemplate,
  FileSearch,
  GitCompareArrows,
  CalendarClock,
} from 'lucide-react';

export const NAV_ICONS = {
  draft: PenLine,
  documents: FolderOpen,
  templates: LayoutTemplate,
  review: FileSearch,
  compare: GitCompareArrows,
  limitation: CalendarClock,
};
